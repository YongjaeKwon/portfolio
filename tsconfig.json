{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ],
  "compilerOptions": {
    "baseUrl": ".", // 절대 경로의 기준 디렉토리
    "paths": {
      "@/*": ["src/*"] // `@/`는 `src/` 디렉토리를 가리킴
    }
  }
}
